<section class="services-container">
  <div class="services-grid">
    <div class="service-card">
      <div class="icon-key"></div>
      <h3>Custom Web Apps</h3>
      <p>Hand-coded logic tailored specifically to your business workflow.</p>
    </div>

    <div class="service-card">
      <div class="icon-key"></div>
      <h3>E-commerce</h3>
      <p>
        Bespoke storefronts designed for smooth, high-conversion transactions.
      </p>
    </div>

    <div class="service-card">
      <div class="icon-key"></div>
      <h3>UI/UX Design</h3>
      <p>Clean, high-contrast interfaces with our signature neon glow.</p>
    </div>

    <div class="service-card">
      <div class="icon-key"></div>
      <h3>Performance</h3>
      <p>
        Ultra-fast load times achieved through lightweight, purposeful code.
      </p>
    </div>

    <div class="service-card">
      <div class="icon-key"></div>
      <h3>Brand Identity</h3>
      <p>Developing a visual language that matches your studio's ambition.</p>
    </div>

    <div class="service-card">
      <div class="icon-key"></div>
      <h3>API Logic</h3>
      <p>Connecting your custom website to the digital tools you use daily.</p>
    </div>
  </div>
</section>

<style>
  /* Container Setup */
  .services-container {
    padding: 4rem 5%;
    background-color: #0b0e14; /* Deep Midnight */
  }

  /* 3x2 Grid Logic */
  .services-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr); /* 3 columns */
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  /* Responsive: 2 columns on tablets, 1 on mobile */
  @media (max-width: 992px) {
    .services-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  @media (max-width: 600px) {
    .services-grid {
      grid-template-columns: 1fr;
    }
  }

  /* Individual Card Styling */
  .service-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(0, 229, 255, 0.1); /* Subtle Cyan Border */
    padding: 2.5rem;
    border-radius: 4px; /* Sharp, professional corners */
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .service-card:hover {
    border-color: #00e5ff; /* Electric Cyan */
    transform: translateY(-5px);
    box-shadow: 0 10px 30px rgba(0, 229, 255, 0.1);
  }

  /* The "Key" Icon Placeholder */
  .icon-key {
    width: 40px;
    height: 40px;
    margin-bottom: 1.5rem;
    background-color: #00e5ff;
    /* You can replace this with an SVG icon later */
    mask: url("/icons/key-icon.svg") no-repeat center;
    -webkit-mask: url("/icons/key-icon.svg") no-repeat center;
  }

  h3 {
    color: #ffffff;
    font-size: 1.5rem;
    margin-bottom: 1rem;
    font-weight: 600;
  }

  p {
    color: #8e9aaf; /* Slate Gray */
    line-height: 1.6;
    font-size: 1rem;
  }
</style>
